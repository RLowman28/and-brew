// Import React and the primary components for this app.
import React from 'react';

// Return a plain old h1 element until the components are tested.
const App: React.FC = () => {
  return <h1>Hang tight, something's coming.</h1>;
}

// Export the App component as the default for use in index.tsx.
export default App;
